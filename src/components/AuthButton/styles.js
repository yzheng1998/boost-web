import theme from '../../theme'

const styles = {
  root: {
    background: 'white',
    borderRadius: 30,
    color: theme.colors.tertiary,
    height: 40,
    width: 180,
    paddingTop: 2,
    paddingBottom: 2
  }
}

export default styles
